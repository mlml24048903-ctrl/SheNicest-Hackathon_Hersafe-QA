# 她测 Web 设计系统

## 设计目标

她测是给产品经理和产品设计者使用的审查工作台。界面首先要帮助用户看清产品功能、代码关系、核查问题和规则依据，而不是展示“AI 很聪明”。

## 视觉原则

- 以白色和冷灰色为主，不使用暖纸张底色。
- 采用系统字体：SF Pro、苹方及系统无衬线字体。
- 页面使用清楚的侧边栏、工具栏和内容分区，参考 macOS 与 iPadOS 默认应用。
- 大圆角只用于主要面板；同一面板内部优先使用留白和分隔线，不连续嵌套卡片。
- 阴影只表示层级，不给普通信息卡添加发光描边。
- 白色承担主要面积；荧光绿只用于焦点、选中和品牌细节，橙红用于风险与提醒。大段正文和大面积面板不铺高饱和色。
- 主按钮使用可读性更高的深绿，荧光绿用于轮廓和反馈，保证白字对比度。
- 不使用极光、渐变文字、光束边框、按钮扫光和无意义的入场动画。

## 信息表达

- 面向用户的文字使用具体中文，避免“能力、闭环、链路、赋能”等抽象词。
- 每条核查内容分为：代码中已经确认、还需要确认、为什么要核查。
- 技术函数名、内部证据编号和模型字段默认折叠。
- 风险线索不写成已经成立的风险；没有发现风险时如实说明。

## 品牌图标

- 品牌动物是像素热带鱼“泡芙”：高背短身、分叉尾、有明显鱼鳍，使用更细的 2px 像素节奏；不使用鲸鱼轮廓，也不借用机器人、星芒或传统女性符号。
- 小鱼代表在复杂材料中寻找线索，也代表灵活、主动和不替用户做决定。女性友好由产品原则和规则表达，不靠粉色、蝴蝶结或被盾牌包围的人像表达。
- 泡芙使用荧光绿身体、橙红尾鳍和白色留白；品牌图标中的泡芙带一枚小放大镜，表示寻找证据。高饱和色只占小面积，侧边栏图标、浏览器图标和助手入口使用同一几何轮廓。
- 图标以原创 SVG 绘制，并使用像素边缘，在浏览器标签、侧边栏和 16px 小尺寸下仍能辨认。

## 素材与动效

- Koboyo 手绘图标仅用于上传提示和空状态。素材本地化保存在 `public/illustrations/koboyo`，同时保留来源与许可说明。
- Dot Matrix 点阵节奏用于上传、解析和生成报告；每次只出现一个主加载动画，并配具体中文状态。
- 按钮按压、菜单展开和成功反馈采用 150–200ms 的轻量微交互。不开启全页飞入或发光扫过。
- 泡芙不使用底框，只在右下角约 380×88px 的扁长范围缓慢游动；尾鳍和上下鱼鳍有独立轻摆，悬停或聚焦时暂停。点击后展开侧边助手，除此之外不增加持续装饰动画。
- `prefers-reduced-motion` 下取消循环和位移动画，只保留最终状态。

## 泡芙助手

- 助手只回答系统使用、步骤含义和规则来源，不假装知道当前项目中没有提供给它的实时事实。
- 助手回答必须来自实时模型；生产环境没有模型密钥时明确提示未配置，不使用预生成答案兜底。
- 抽屉用原生对话框承载，支持 Escape 关闭、键盘聚焦和关闭后焦点回到小鱼入口。
- 推荐问题纵向排列，不使用横向滚动。

## 页面结构

- 应用外壳固定，工作台页面不滚动整个浏览器，只滚动当前内容区。
- 四步导航保持扁平，当前步骤说明只出现一次。
- 初步分析左侧显示真实产品流程，右侧只显示待办入口。
- 待办详情依次为标题、代码与规则、补充确认对话。
- 报告使用单一正文滚动区，避免多层滚动。
